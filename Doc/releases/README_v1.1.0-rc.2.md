# GuaranteeManager v1.1.0-rc.2

هذه ثاني نسخة مرشحة لإطلاق `v1.1.0`. جاءت بعد `rc.1` لتنظيف بقايا مراحل سابقة، تثبيت النوافذ الفرعية، وتحديث التوثيق والجذر قبل الانتقال إلى الإطلاق النهائي.

> **مهم:** هذه نسخة Release Candidate. لا تضيف ميزات جديدة فوقها إلا إذا كانت إصلاحًا مانعًا ومحدود الأثر.

## حالة هذه النسخة

- رقم النسخة داخل التطبيق: `v1.1.0-rc.2`
- نوع الإصدار: **Release Candidate**
- الغرض: تثبيت خط `v1.1.0` بعد تنظيف المسارات القديمة وترتيب المشروع
- اسم الحزمة: `GuaranteeManager_v1.1.0-rc.2_win-x64.zip`
- مجلد النشر: `releases/v1.1.0-rc.2`

## ما تغيّر بعد rc.1

- إزالة مسار الاستعلام التشغيلي القديم وطبقة `ContextAction` غير المستخدمة.
- تثبيت قائمة النوافذ الفرعية القابلة للوصول من الواجهة وتدقيقها بصريًا.
- توحيد مظهر النوافذ الفرعية مع الواجهات الرئيسية من ناحية الكروم، الحدود، المسافات، والخطوط.
- تنظيف توثيق الإصدار والحالة الحالية وخريطة المستندات.
- إضافة `README.md` في جذر المشروع.
- تنظيف الجذر من مخرجات Excel المولدة ونقلها إلى `scratch/`.
- إبقاء العمل اليومي محصورًا في الواجهات الخمس الرسمية.

## نطاق التجميد

- الواجهة الرسمية الحالية هي `اليوم`، `الضمانات`، `البنوك`، `التقارير`، و`الإعدادات`.
- لا توجد واجهة مستقلة للطلبات أو التنبيهات أو ملف الضمان.
- الطلبات وردود البنك والمرفقات تظهر وتدار من السجل الزمني، اللوحة الجانبية، اليوم، والتقارير.
- المستفيد ثابت: `مستشفى الملك فيصل التخصصي ومركز الأبحاث`.
- المورد هو الطرف المتغير الذي يظهر للمستخدم في الجداول، البحث، التقارير، ومخرجات Excel.

## قواعد العمل المثبتة

- طلب النقض ملغى تشغيليًا.
- الضمان المنتهي لا يقبل إلا الإفراج/الإعادة للبنك.
- التمديد والتخفيض يمكن أن يتواليا على الضمان النشط غير المنتهي.
- الإفراج والتسييل والاستبدال تنهي دورة حياة الضمان عند تأكيد البنك.
- تنفيذ إجراء نهائي يلغي الطلبات العالقة الأخرى على نفس الجذر.
- التعديل الإداري لا يغير المبلغ أو تاريخ الانتهاء أو البنك أو رقم الضمان أو النوع أو المرجع.
- السجل الزمني هو مصدر الحقيقة لدورة حياة الضمان.

## حالة التحقق

- `dotnet build GuaranteeManager.csproj --no-restore` ناجح: `0 warnings / 0 errors`.
- `dotnet test GuaranteeManager.Tests\GuaranteeManager.Tests.csproj --no-restore` ناجح: `137/137`.
- `.\scripts\publish_release.ps1` ناجح وأنشأ الحزمة المحلية، مع اختبارات Release ناجحة: `134/134`.
- تدقيق النوافذ الفرعية الفعلي غطى `12` نافذة قابلة للوصول من الواجهة.

## تحقق محلي بعد التنظيف

- `dotnet build GuaranteeManager.csproj` ناجح: `0 warnings / 0 errors`.
- `dotnet test GuaranteeManager.Tests\GuaranteeManager.Tests.csproj` ناجح: `139/139`.
- أعيد ترتيب التوثيق: النشط في `Doc/`, التصميم في `Doc/design/`, التدقيق في `Doc/audits/`, والقديم في `Doc/archive/`.

## طريقة التشغيل

1. فك ضغط `GuaranteeManager_v1.1.0-rc.2_win-x64.zip`.
2. افتح المجلد الناتج `v1.1.0-rc.2`.
3. شغّل `GuaranteeManager.exe`.

## الانتقال القادم

- إذا ظهر خلل مانع: إصلاح محدود ثم `v1.1.0-rc.3`.
- إذا بقيت المراجعة نظيفة: إطلاق `v1.1.0`.
- أي تحسينات أو أفكار جديدة بعد هذه النقطة تذهب إلى فرع المرحلة القادمة.
