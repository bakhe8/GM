# Preview 2 Polish Backlog

**تاريخ الإنشاء:** 2026-04-27  
**المرجع:** هذه backlog polish للنسخة `v1.1.0-preview.2` بعد إغلاق parity مع الواجهة القديمة.  
**ليست** backlog فجوات من القديم إلى الجديد، ولا بديلًا عن `missing_features_report.md`.

## حالة التنفيذ الحالية

- `لوحة التحكم`: نُفذت الجولة الأولى وصارت الأفعال اليومية تقود مباشرة إلى الملف أو القسم المناسب.
- `لوحة التحكم`: أُنجزت مراجعة بصرية لاحقة، وصارت أزرار الصف نفسها تسمي المساحة المقصودة (`التنبيهات` / `الطلبات`) بدل الفعل العام `انتقال`.
- `ملف الضمان`: نُفذت جولتا ترتيب الأفعال والإخفاء الذكي.
- `الطلبات`: نُفذت جولة `queue-first` وأصبح الفعل اليومي ديناميكيًا حسب حالة الطلب.
- `الاكتمال الصامت`: نُفذت الجولة الأولى على `الطلبات` و`الضمانات` عبر قناة حالة هادئة في الـ `Shell`.
- `الحوارات الثقيلة`: نُفذت جولة أولى لتخفيف نجاحات `سجل الضمان` و`الاستعلامات التشغيلية` و`طلبات الضمان` داخل الحوارات نفسها.
- `المراجعة البصرية التشغيلية`: تأكدنا بصريًا من ظهور التغذية الهادئة في `الطلبات`، وأغلقنا فجوة نسخ رقم الضمان من `HistoryDialog` بحيث صارت تستخدم الشريط السفلي نفسه بدل النسخ الصامت الكامل.
- `طلبات الضمان`: أُغلقت فجوة صغيرة داخل الحوار نفسه، وصارت أزرار `تسجيل رد / فتح الخطاب / فتح الرد` تتبع صلاحية الطلب المحدد بدل أن تبدو متاحة دائمًا.
- `RequestsDialog`: أُضيف زر `إغلاق` واضح، وصار `Escape` يغلق الحوار كما يتوقع المستخدم.
- `OperationalInquiryDialog`: أُغلقت فجوة توضيح صغيرة، وصارت الأزرار المعطلة تعرض سبب التعطيل عبر tooltips بدل التعطيل الصامت الكامل.
- `OperationalInquiryDialog`: أُنجزت مراجعة `الجواب ثم الفعل`:
  - أُضيفت بطاقة `الخطوة التالية` داخل لوحة السياق اليمنى
  - وصارت ترشح الفعل الأنسب حسب الحالة (`فتح رد البنك` / `فتح خطاب الطلب` / `فتح مرفقات الإصدار` / `فتح السجل` / `تقرير الضمان`)
  - وثُبت حيًا أن الزر المقترح يفتح المسار التالي الفعلي كما ينبغي
- `NewGuaranteeDialog` و`EditGuaranteeDialog`: أُنجزت معاينة الأثر، وثُبتت حيًا:
  - `إجراء جديد` يبدأ بحالة منع واضحة ثم ينتقل إلى `سيُنشأ ضمان جديد كإصدار أول (v1)` عند اكتمال الحد الأدنى من البيانات
  - `تعديل الضمان` يبدأ بحالة `لا توجد تغييرات` ثم ينتقل إلى `سيُنشأ إصدار جديد v2` عند تعديل حقل واحد
- `HistoryDialog`: أُنجزت مراجعة "الخطوة التالية"، وصار الحوار يقدّم بطاقة سياقية واضحة داخل اللوحة اليمنى:
  - في تبويب الإصدارات: يرشح `فتح مرفقات الإصدار` أو `تصدير السجل` حسب الحالة
  - في تبويب الطلبات: يرشح `فتح خطاب الطلب` أو `فتح رد البنك` أو `عرض الإصدار الناتج/الأساس`
  - كما صارت تسميات `الإصدار الأساس/الناتج` تعرض `VersionLabel` البشري بدل القيم الداخلية الخام
- الحصر الحالي يقول إن المتبقي الفعلي صار صغيرًا ويتمحور الآن في:
  - استكمال جولة أداء بعد UAT أوسع، بعد أن أغلقت الجولة الأولى اختناقًا واضحًا في الأداة نفسها

## الحصر الحالي المختصر

بعد الجولات المنجزة حتى الآن، لم تعد هذه backlog كبيرة كما بدأت. ما بقي فعلاً هو:

### A. جولة الأداء بعد UAT أوسع

من Logs الأداة نفسها، وبعد أول UAT أوسع على بيانات مولدة من داخل `Settings`، صار الحكم أدق:

- البطء الأوضح المتبقي لم يعد في `Launch`
- وهو يتمركز أكثر في:
  - مسار القوائم المنبثقة داخل `Settings`
  - ظهور نوافذ التأكيد بعد أدوات destructive مثل `seed-development-data`
  - وبعض `Click` المرتبطة بانتظار ظهور dialog أو popup

هذه ليست polish واجهة بصرية مباشرة، لكنها بقيت ضمن polish الإيقاع العام ويجب أخذها **بعد** جولة UAT أوسع، لا قبلها.

**الحالة الحالية**
- أُنجزت الآن `Performance Pass 01`
- وفيها:
  - استُبدل نوم `Sidebar` الثابت بانتظار ذكي يعتمد على `ShellState`
  - خُففت delays الثابتة بعد بعض أنماط التفاعل
  - وأضيفت عتبات زمنية حسب نوع الإجراء (`Launch` / `Sidebar` / `Click` / `DialogAction`)
- النتيجة المباشرة:
  - `Sidebar` تحسنت من ~`2072ms` إلى ~`1146ms` في الجولة الأوسع
  - `Click` داخل الحوارات الثقيلة نزلت إلى ~`400–600ms`
  - `Launch` ظهرت في دورة نظيفة عند ~`3617ms` ولم تعد تُصنَّف بطئًا كاذبًا
- المرجع الكامل هنا:
  - [UIAcceptance/baselines/2026-04-27-performance-pass-01.md](c:/Users/Bakheet/Documents/Projects/Work/my_work/Doc/Assets/Documentation/Screenshots/UIAcceptance/baselines/2026-04-27-performance-pass-01.md)
- كما توجد جولة UAT مرتبطة مباشرة بهذا الحكم هنا:
  - [UIAcceptance/baselines/2026-04-27-heavy-seed-uat-pass-01.md](c:/Users/Bakheet/Documents/Projects/Work/my_work/Doc/Assets/Documentation/Screenshots/UIAcceptance/baselines/2026-04-27-heavy-seed-uat-pass-01.md)

**أهم مخرج من الجولة**

- لم تكن المشكلة مجرد بطء: الجولة كشفت bug حقيقيًا بعد reseed
- `Shell` كانت تحتفظ بضمان محدد stale بعد إعادة بناء قاعدة البيانات
- أُغلق هذا بإضافة refresh مركزي بعد نجاح `SeedDevelopmentData`
- وبعد الإصلاح، عاد `OperationalInquiryDialog` ليفتح بنجاح على البيانات الجديدة

## كيف نستخدم هذه القائمة

- هذه القائمة مخصصة لتحسين:
  - وضوح الدور
  - وضوح الخطوة التالية
  - تقليل العبء
  - والاكتمال الصامت
- لا نضيف إليها bugs تشغيلية عابرة إلا إذا تكررت وصارت تمس نضج التجربة.
- الترتيب هنا يعكس ما ينبغي أن ندفعه أولًا، لا مجرد ما يمكن فعله أسرع.

---

## الآن — الدفعة التالية مباشرة

### 1. رفع دور لوحة التحكم من Inbox إلى "ابدأ يومك"

**الهدف**
- جعل السطح يعبّر عن `ما الذي أفعله الآن؟` أكثر من `ما الذي أفتحه الآن؟`

**المطلوب**
- تقوية CTA لكل عنصر عندما يكون القرار واضحًا
- تخفيف وزن بطاقات الأرقام أمام بطاقات العمل
- مراجعة بطاقة `آخر ملف` لتكون نقطة استئناف أوضح

**المقياس**
- المستخدم يرى من أول شاشة:
  - العمل الأهم
  - لماذا هو مهم
  - وما الخطوة التالية

**الحالة الحالية**
- أُنجزت الجولة الأولى
- ثم أُنجزت مراجعة بصرية ثانية على أزرار الصف
- وبالتالي لم يعد هذا بندًا مفتوحًا كبيرًا، بل يحتاج فقط مراقبة خلال UAT بدل تطوير مباشر الآن

### 2. ترتيب الإجراءات في ملف الضمان حسب الأولوية الفعلية

**الهدف**
- منع تساوي كل الأفعال بصريًا داخل قسم الإجراءات السريعة

**المطلوب**
- فصل:
  - الأفعال الأساسية المتوقعة الآن
  - الأفعال الثانوية
- إبراز `محور الإجراء الآن` أكثر من بقية البطاقات

**المقياس**
- من النظر الأول، نعرف:
  - ما العمل التالي
  - وما الأفعال المتاحة فقط عند الحاجة

**الحالة الحالية**
- أُنجز هذا البند فعليًا في جولتين
- وما بقي هو مراقبة الإحساس العام فقط أثناء UAT

### 3. زيادة الإخفاء الذكي في ملف الضمان

**الهدف**
- تقليل الاعتماد على disabled actions عندما لا يكون الفعل منطقيًا أصلًا

**المطلوب**
- مراجعة أي الأفعال:
  - يجب أن يختفي
  - وأيها يجب أن يبقى ظاهرًا لكن مع hint

**المقياس**
- تقل كثافة الأزرار
- وتتحسن نسبة “المعروض الآن = المفيد الآن”

**الحالة الحالية**
- أُنجزت الجولة الأساسية
- وما بقي هو اكتشاف حالات استثنائية فقط إذا ظهرت أثناء الاستخدام الواقعي

### 4. تنقية شاشة الطلبات لتبقى queue-first

**الهدف**
- تقليل شعور الشاشة بأنها تجمع queue + batch tools + create flows بنفس الوزن

**المطلوب**
- إبراز الفعل التالي حسب الحالة أكثر
- مراجعة الـ toolbar:
  - ما الذي يبقى مباشرًا
  - وما الذي يخرج إلى قوائم ثانوية

**المقياس**
- المستخدم يقرأ الشاشة كـ queue عمل أولًا، لا كلوحة أدوات عامة

**الحالة الحالية**
- أُنجز هذا البند في الجولة الأساسية
- ثم تحسنت الحوارات التابعة له (`RequestsDialog`) من زاوية الصلاحية والخروج
- لذا لم يعد بندًا مفتوحًا بذاته الآن

### 5. تقليل رسائل النجاح modal عندما لا تكون ضرورية

**الهدف**
- زيادة الاكتمال الصامت وبقاء المستخدم في السياق

**المطلوب**
- مراجعة مسارات النجاح في:
  - الطلبات
  - الضمانات
  - الحوارات الثقيلة
- تحديد ما يمكن تحويله من:
  - `رسالة نجاح`
  - إلى `تحديث هادئ + بقاء في نفس الموضع`

**المقياس**
- عدد أقل من الرسائل التي تعطل الإيقاع
- مع بقاء وضوح النجاح للمستخدم

**الحالة الحالية**
- تم تنفيذ هذا البند جزئيًا في:
  - `الطلبات`
  - `الضمانات`
- كما امتد الآن إلى نجاحات متكررة داخل:
  - `HistoryDialog`
  - `OperationalInquiryDialog`
  - `RequestsDialog`
- ثم أُغلق فرق صغير في `HistoryDialog` بحيث صار `نسخ رقم الضمان` نفسه يمر عبر القناة الهادئة أيضًا.
- وما بقي منه الآن هو تطبيق نفس الفكرة على المسارات الثقيلة التي ما زالت تستفيد من dialog أوضح من مجرد status bar.

---

## بعد ذلك — دفعة polish ثانية

### 6. مراجعة `HistoryDialog` من زاوية الخطوة التالية

**الهدف**
- التأكد أن الحوار لا يكتفي بعرض السجل، بل يقود إلى الفعل التالي بوضوح نفسي وبصري

**المطلوب**
- مراجعة توزيع:
  - الملخص
  - الإصدارات/الطلبات
  - لوحة التفاصيل
  - أزرار الفعل
- والتأكد أن الانتقال من الفهم إلى القرار لا يزال واضحًا في القراءة الأولى

**المقياس**
- من داخل السجل، نعرف بسهولة ما الذي يمكن فعله بعد الفهم

---

## لاحقًا — بعد UAT حقيقي أوسع

### 7. مراجعة الأداء وسرعة الإيقاع داخل التدفقات المتكررة

**الهدف**
- بعد تثبيت دور الشاشات، نعود إلى السرعة والاحتكاك

**المطلوب**
- قياس:
  - كثافة الرسائل
  - عدد الخطوات
  - زمن الانتقال
  - كثافة التحديثات المرئية

### 9. تحويل ملاحظات UAT المتكررة إلى polish narrative لا إلى bug list فقط

**الهدف**
- إذا تكررت ملاحظة مثل:
  - "هذا مربك"
  - "هذه الخطوة ثقيلة"
  - "هذا الفعل غير واضح"
- تتحول إلى تحسين تجربة منظم، لا مجرد رقعة موضعية

---

## ما الذي لا يدخل هذه القائمة

- فجوات parity القديمة
- bugs تشغيلية معزولة لمرة واحدة
- refactors معمارية لا تغير التجربة
- تحسينات شكلية لا تخدم:
  - الأولوية
  - السياق
  - أو الخطوة التالية

---

## الترتيب المعتمد

إذا أردنا التنفيذ على دفعات قصيرة، فالترتيب المقترح هو:

1. `OperationalInquiryDialog`
2. `Performance pass` بعد UAT أوسع

## الخلاصة

من هنا فصاعدًا، نجاح `preview.2` لا يقاس بوجود الوظائف فقط.  
يقاس بقدرتنا على جعل هذه الأسطح الأربع:

- أوضح
- أهدأ
- أذكى
- وأقل طلبًا لعبء ذهني من المستخدم
