# حالة المشروع — GuaranteeManager
**تاريخ آخر تدقيق:** 2026-04-23
**نوع التقرير:** نسخة مدققة بناءً على اللقطة الحالية للمشروع
**المرجع التنفيذي للإطلاق الأول:** `release_v1.md`

---

## 1. النتيجة المختصرة

### مثبت عمليًا

- `Release build`: ينجح بـ **0 أخطاء، 0 تحذيرات**
- الاختبارات: **34/34 ناجحة**
- الاعتماديات الأساسية متطابقة مع ملف المشروع `GuaranteeManager.csproj`

### تحفظات مهمة

- يوجد داخل شجرة المشروع فعليًا الملفان `Data/.dbkey` و`Data/guarantees.db`
- المجلد الحالي **ليس مستودع Git مهيأ**، لذلك لا يمكن الجزم بتاريخ التتبع أو نظافة السجل
- بقي أثر واجهة غير منظف بالكامل: المعرف `inquiry.export` ما زال موجودًا في `Views/InquiryResultWindow.xaml.cs`

> بناءً على ذلك: يمكن القول إن المشروع **يبني ويجتاز الاختبارات**، لكن وصفه بأنه "نظيف بالكامل وجاهز للإصدار" يحتاج تحفظًا.

---

## 2. ما تم التحقق منه داخل الكود

### أ) إصلاحات البناء
| البند | الحالة |
|------|--------|
| تغليف `using GuaranteeManager.Development` بـ `#if DEBUG` في `App.xaml.cs` و`Views/SettingsView.xaml.cs` | **مثبت** |

### ب) حماية البيانات والتخزين
| البند | الحالة |
|------|--------|
| `.gitignore` يستثني `Data/*.db`, `Data/.dbkey`, `Logs/`, `Data/window-state.json` | **مثبت** |
| تشفير قاعدة البيانات يعتمد على `SQLCipher` + `Windows DPAPI` | **مثبت** |
| النسخ الاحتياطي يتحقق من `PRAGMA quick_check` ويقارن عدد كائنات الـ schema | **مثبت** |
| التطبيق يستخدم `Mutex` لمنع تشغيل نسختين متزامنتين | **مثبت** |

### ج) قاعدة البيانات وسير العمل
| البند | الحالة |
|------|--------|
| `GetAttachmentsForGuarantee` يستخدم قائمة أعمدة صريحة بدل `SELECT *` | **مثبت** |
| `RotateBackups` يرتب باسم الملف بدل `CreationTime` | **مثبت** |
| تنفيذ النقض يتطلب مستند رد البنك إلزاميًا | **مثبت** |
| `NormalizeGuaranteeRoots` موجود ويُستدعى حاليًا في أكثر من موضع، وليس داخل `CreateNewVersion` فقط | **مثبت** |

### د) الاختبارات الحالية

- `34` اختبارًا ناجحًا موزعة حاليًا إلى:
- `14` في `DatabaseWorkflowSmokeTests`
- `12` في `GuaranteeQueryTests`
- `6` في `ShellViewModelTests`
- `2` في `WorkspaceReportCatalogTests`

> عبارة "34 اختبار تكاملي حقيقي" **غير دقيقة حرفيًا**؛ العدد صحيح، لكن النوع مختلط بين اختبارات قاعدة بيانات/تكامل واختبارات سلوك ViewModel.

---

## 3. عناصر لم تعد موجودة في الشجرة الحالية

الملفات التالية **غير موجودة الآن**:

- `Views/CountToVisibilityConverter.cs`
- `Utils/ComboBoxAssist.cs`
- `Assets/logo.png`
- `Assets/KFSHRC/logo.svg`
- `Assets/Documentation/Screenshots/README.md`

> لكن حذف `Assets/Documentation/Screenshots/README.md` ترك روابط مكسورة في:
> `User_Guide_Final.md` و`Daily_Quick_Guide.md`

---

## 4. تصحيحات على التقرير السابق

- العبارة "لا توجد ملفات تشغيلية أو مفاتيح تشفير داخل شجرة الكود" **غير صحيحة** في هذه اللقطة؛ لأن `Data/.dbkey` و`Data/guarantees.db` موجودان فعليًا.
- العبارة "ملفا `Data/.dbkey` و`Data/guarantees.db` غير متتبعين في أي مستودع" **غير قابلة للإثبات من هذه اللقطة**؛ لأن المجلد الحالي لا يحتوي `.git`.
- العبارة التي تقول إن `NormalizeGuaranteeRoots` بقيت فقط داخل `CreateNewVersion` **غير دقيقة**؛ توجد استدعاءات أخرى في `GuaranteeSchemaManager` و`WorkflowExecutionDataAccess`.
- العبارة التي تقول إن `inquiry.export` حُذف من الواجهة **غير دقيقة**؛ المعرّف ما زال موجودًا في `Views/InquiryResultWindow.xaml.cs`.
- وصف المشروع بأنه "جاهز للإصدار" يحتاج **تحفظًا**: البناء والاختبارات ناجحان، لكن التقرير السابق بالغ في وصف النظافة التشغيلية/المستندية.

---

## 5. نقاط قوة مثبتة في النظام الحالي

- **Versioning** موجود فعليًا عبر `RootId`, `VersionNumber`, `IsCurrent`
- **التشفير المحلي** موجود عبر `SQLCipher` و`DPAPI`
- **النسخ الاحتياطي المتحقق** موجود ويستخدم `quick_check`
- **الحزم المحمولة** مدعومة ومغطاة باختبار استرجاع فعلي
- **منع التكرار** مغطى باختبار رقم الضمان بعد التطبيع
- **منع تعدد التشغيل** موجود عبر `Mutex`

---

## 6. بنود مفتوحة ما زالت واقعية

### أولوية عالية
| البند | الملاحظة |
|------|----------|
| تقوية Passphrase للحزمة المحمولة | التحقق الحالي يفرض فقط 8 أحرف فأكثر |
| تنظيف أثر `inquiry.export` | ما زال موجودًا كمعرّف واجهة |
| مراجعة وضع الملفات التشغيلية داخل الشجرة | `Data/.dbkey` و`Data/guarantees.db` موجودان محليًا داخل المشروع |

### أولوية متوسطة
| البند | الملاحظة |
|------|----------|
| حالة `Closed` | معرفة في النموذج والتقارير، لكن لم أجد workflow يعيّنها |
| تكرار `PRAGMA table_info` | موجود عدة مرات في `GuaranteeSchemaManager` |
| روابط توثيق مكسورة | بسبب غياب `Assets/Documentation/Screenshots/README.md` |

### بنيوي
| البند | الملاحظة |
|------|----------|
| Code-behind ضخم | `TodayDeskView` 1215 سطرًا، `GuaranteeFileView` 1074، `DataTableView` 997، `OperationCenterView` 1009 |
| منطق "منتهٍ وما زال نشطًا" | ما زالت توجد إشارات صريحة لهذا السيناريو في الاستعلامات والتقارير |

---

## 7. ملفات تحتاج قرارًا تنظيميًا

| الملف | الوضع الحالي |
|------|--------------|
| `User_Guide_Final.md` | موجود في جذر المشروع |
| `Daily_Quick_Guide.md` | موجود في جذر المشروع |
| `design_system.md` | موجود في جذر المشروع |

---

## 8. الاعتماديات المثبتة

| الحزمة | الاستخدام |
|--------|----------|
| `Microsoft.Data.Sqlite.Core 10.0.6` | طبقة البيانات |
| `SQLitePCLRaw.bundle_e_sqlcipher 2.1.10` | تشفير قاعدة البيانات |
| `ClosedXML 0.105.0` | تقارير Excel |
| `Microsoft.Extensions.DependencyInjection 10.0.0` | حقن الاعتماديات |
