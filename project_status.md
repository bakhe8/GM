# حالة المشروع — GuaranteeManager
**تاريخ آخر تدقيق:** 2026-04-23
**نوع التقرير:** نسخة مدققة بناءً على اللقطة الحالية للمشروع
**المرجع السريع:** `CURRENT_STATE.md`
**المرجع التنفيذي للإطلاق الأول:** `release_v1.md`
**الحالة الحالية في Git:** baseline موسوم `v1.0.0`، و`main` عند `d855be1`، والفرع النشط `feature/v1.1-operational-polish` مع آخر دفعة برمجية مستقرة عند `604bfd9`

---

## 1. النتيجة المختصرة

> **ملاحظة تحديث:** تم دمج دفعة `v1.1-kickoff` إلى `main`، وعلى الفرع النشط
> `feature/v1.1-operational-polish` أضيفت أدوات تشغيل جديدة في شاشة الإعدادات
> لفتح مجلد التنفيذ ونسخ ملخص المسارات.

### مثبت عمليًا

- `Release build`: ينجح بـ **0 أخطاء، 0 تحذيرات**
- الاختبارات: **46/46 ناجحة**
- الاعتماديات الأساسية متطابقة مع ملف المشروع `GuaranteeManager.csproj`
- المشروع أصبح مستودع Git مهيأ محليًا ومرتبطًا بـ GitHub مع baseline موسوم `v1.0.0`
- `main` يحمل الآن دفعة `v1.1-kickoff` المدموجة، بينما الفرع النشط يحمل تحسينًا إضافيًا صغيرًا لم يُدمج بعد
- شاشة الإعدادات تعرض الآن جذر التخزين الفعلي ومجلد التنفيذ الحالي بشكل صريح، وتتيح فتح مجلد التنفيذ ونسخ ملخص مسارات التشغيل
- سيناريو المتابعة بعد الانتهاء صار موحدًا على هذا الفرع تحت تسمية: `منتهية وتحتاج متابعة`
- الشجرة المحلية الحالية نُظفت من بقايا التشغيل (`Data` و`Attachments` و`Workflow` و`Logs`) مع الاحتفاظ بنسخة backup خارج المشروع

### تحفظات مهمة

- يجب مراقبة أي بقايا تشغيل محلية جديدة إذا ظهرت في بيئات تطوير أخرى، لكن الحالة المحلية الحالية أصبحت نظيفة
- من واقع تجربة استخدام كاملة حديثة: الخلل الأوضح لم يعد وظيفيًا، بل **ضعف عملية الواجهة ووضوح مسارات الوصول** رغم اكتمال الوظائف الأساسية

> بناءً على ذلك: يمكن القول إن المشروع **يبني ويجتاز الاختبارات**، ومع حالة الشجرة المحلية الحالية صار أوضح وأقرب إلى النظافة التشغيلية من اللقطات السابقة.

---

## 2. ما تم التحقق منه داخل الكود

### أ) إصلاحات البناء
| البند | الحالة |
|------|--------|
| تغليف `using GuaranteeManager.Development` بـ `#if DEBUG` في `App.xaml.cs` و`Views/SettingsView.xaml.cs` | **مثبت** |

### ب) حماية البيانات والتخزين
| البند | الحالة |
|------|--------|
| `.gitignore` يستثني `Data/*.db`, `Data/.dbkey`, `Logs/`, `Data/window-state.json` | **مثبت** |
| تشفير قاعدة البيانات يعتمد على `SQLCipher` + `Windows DPAPI` | **مثبت** |
| النسخ الاحتياطي يتحقق من `PRAGMA quick_check` ويقارن عدد كائنات الـ schema | **مثبت** |
| التطبيق يستخدم `Mutex` لمنع تشغيل نسختين متزامنتين | **مثبت** |

### ج) قاعدة البيانات وسير العمل
| البند | الحالة |
|------|--------|
| `GetAttachmentsForGuarantee` يستخدم قائمة أعمدة صريحة بدل `SELECT *` | **مثبت** |
| `RotateBackups` يرتب باسم الملف بدل `CreationTime` | **مثبت** |
| تنفيذ النقض يتطلب مستند رد البنك إلزاميًا | **مثبت** |
| `NormalizeGuaranteeRoots` موجود ويُستدعى حاليًا في أكثر من موضع، وليس داخل `CreateNewVersion` فقط | **مثبت** |

### د) الاختبارات الحالية

- `46` اختبارًا ناجحًا موزعة حاليًا إلى:
- `2` في `AppPathsTests`
- `14` في `DatabaseWorkflowSmokeTests`
- `3` في `GuaranteeExpiryFollowUpTests`
- `2` في `GuaranteeLifecycleStatusDisplayTests`
- `12` في `GuaranteeQueryTests`
- `1` في `OperationalPathsSummaryFormatterTests`
- `2` في `PortableBackupPackageCryptoTests`
- `6` في `ShellViewModelTests`
- `1` في `TodayDeskViewModelTests`
- `2` في `WorkspaceReportCatalogTests`

> عبارة "اختبارات تكاملية فقط" **غير دقيقة حرفيًا**؛ العدد الحالي صحيح، لكن النوع مختلط بين اختبارات قاعدة بيانات/تكامل واختبارات سلوك ViewModel واختبارات تحقق موجهة.

---

## 3. عناصر لم تعد موجودة في الشجرة الحالية

الملفات التالية **غير موجودة الآن**:

- `Views/CountToVisibilityConverter.cs`
- `Utils/ComboBoxAssist.cs`
- `Assets/logo.png`
- `Assets/KFSHRC/logo.svg`

---

## 4. تصحيحات على التقرير السابق

- العبارة "لا توجد ملفات تشغيلية أو مفاتيح تشفير داخل شجرة الكود" كانت **خاطئة في لقطة سابقة** لأن `Data/.dbkey` و`Data/guarantees.db` كانا موجودين داخل الشجرة آنذاك. أما الحالة المحلية الحالية فقد نُظفت من هذه البقايا ونُقلت إلى backup خارجي.
- العبارة "ملفا `Data/.dbkey` و`Data/guarantees.db` غير متتبعين في أي مستودع" لا تكفي وحدها الآن لقراءة الوضع الحالي؛ المشروع أصبح مستودع Git فعليًا، كما أن الشجرة المحلية الحالية لم تعد تحتوي هذين الملفين.
- العبارة التي تقول إن `NormalizeGuaranteeRoots` بقيت فقط داخل `CreateNewVersion` **غير دقيقة**؛ توجد استدعاءات أخرى في `GuaranteeSchemaManager` و`WorkflowExecutionDataAccess`.
- العبارة التي تقول إن `inquiry.export` حُذف من الواجهة **لم تعد مطبقة على هذا الفرع**؛ تم تنظيف الأثر المتبقي واستبداله بالمعرّف الفعلي المستخدم.
- وصف المشروع بأنه "جاهز للإصدار" يحتاج **ربطًا بالسياق**: baseline `v1.0.0` أُطلق واعتمد بالفعل، أما الوضع الحالي فهو مسار ما بعد الإطلاق على `main` وفرع `feature` جديد قيد التحسين.

---

## 5. نقاط قوة مثبتة في النظام الحالي

- **Versioning** موجود فعليًا عبر `RootId`, `VersionNumber`, `IsCurrent`
- **التشفير المحلي** موجود عبر `SQLCipher` و`DPAPI`
- **النسخ الاحتياطي المتحقق** موجود ويستخدم `quick_check`
- **الحزم المحمولة** مدعومة ومغطاة باختبار استرجاع فعلي
- **عبارة مرور الحزم المحمولة الجديدة** أصبحت أقوى: `12` حرفًا على الأقل مع حرف ورقم ورمز خاص
- **عزل التخزين** مثبت باختبارات تؤكد المسار الافتراضي خارج مجلد التنفيذ ودعم نسخ المحتوى القديم إلى جذر التخزين المعتمد
- **ملخص مسارات التشغيل** يمكن نسخه الآن مباشرة من شاشة الإعدادات لاستخدامه في الدعم والتحقق السريع من البيئة
- **منع التكرار** مغطى باختبار رقم الضمان بعد التطبيع
- **منع تعدد التشغيل** موجود عبر `Mutex`
- **خط الأساس الإصدار الأول** محفوظ الآن في Git محليًا وعلى GitHub عبر الوسم `v1.0.0`
- **الحالة `Closed`** أصبحت موضحة على هذا الفرع كحالة legacy للتوافق مع البيانات القديمة
- **الضمانات المنتهية التي تحتاج متابعة** أصبحت معرفة بقاعدة موحدة داخل الموديل والتقارير ولوحة المتابعة
- **تنظيف البقايا المحلية** أصبح له سكربت صيانة آمن: `scripts/cleanup_legacy_runtime_tree.ps1`

---

## 6. بنود مفتوحة ما زالت واقعية

### أولوية عالية
| البند | الملاحظة |
|------|----------|
| مراقبة البيئات الأخرى | إذا ظهرت بقايا تشغيل محلية في أي نسخة عمل أخرى، يُستخدم سكربت التنظيف الآمن بدل الحذف اليدوي |
| تبسيط الواجهة ومسارات العمل | التجربة التشغيلية الأخيرة أكدت أن الوظائف موجودة، لكن الوصول إليها مربك ويحتاج إعادة تنظيم على مستوى المهام ونقاط الدخول |

### أولوية متوسطة
| البند | الملاحظة |
|------|----------|
| مراجعة أثر التسمية الجديدة في التشغيل | تم توحيد السيناريو إلى `منتهية وتحتاج متابعة` ويستحق مراقبة استخدامه الفعلي |

### بنيوي
| البند | الملاحظة |
|------|----------|
| Code-behind ضخم | `TodayDeskView` 1215 سطرًا، `GuaranteeFileView` 1074، `DataTableView` 997، `OperationCenterView` 1009 |

---

## 7. ملفات تحتاج قرارًا تنظيميًا

| الملف | الوضع الحالي |
|------|--------------|
| `User_Guide_Final.md` | موجود في جذر المشروع |
| `Daily_Quick_Guide.md` | موجود في جذر المشروع |
| `design_system.md` | موجود في جذر المشروع |

---

## 8. الاعتماديات المثبتة

| الحزمة | الاستخدام |
|--------|----------|
| `Microsoft.Data.Sqlite.Core 10.0.6` | طبقة البيانات |
| `SQLitePCLRaw.bundle_e_sqlcipher 2.1.10` | تشفير قاعدة البيانات |
| `ClosedXML 0.105.0` | تقارير Excel |
| `Microsoft.Extensions.DependencyInjection 10.0.0` | حقن الاعتماديات |
